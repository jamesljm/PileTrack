name: Deploy Staging
on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Render (Staging)
        run: |
          echo "Trigger Render deploy hook for staging"
          # curl -X POST "${{ secrets.RENDER_STAGING_DEPLOY_HOOK }}"
